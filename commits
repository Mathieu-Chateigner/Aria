feat: campaign name flex layout, joincode repositioned, vials alchemy-gated

Changes:

- views/aria-gm.html : moved topbar-joincode to just before the config button (right side); removed topbar-spacer; removed oninput from campaign-name-input (now uses flex layout instead)
- views/aria-player.html : restored inv-vials-section div in inventory tab
- css/aria-gm.css : campaign-name-input now flex:1 (fills all available space), font-size raised to 17px; topbar-joincode color changed to #d4aaff
- js/aria-gm.js : removed autoSizeCampaignInput (no longer needed with flex layout)
- js/aria-player.js : restored renderVialsInInventory() with playerTabs.alchemy guard — vials show in inventory only when GM has granted alchemy access; added call in applyTabVisibility() so it updates immediately when GM toggles the tab
