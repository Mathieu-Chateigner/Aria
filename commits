Pending commit

Changes:

- index.html : Added "Code campagne (joueur)" input field to the config panel. Reads and saves cfg.campaignKey in aria-config.

- views/aria-player.html : Removed campaign key input from character creation form. Removed campaign key field from character editor (Personnage tab). Added campaign key input to the in-app config modal.

- js/aria-player.js : Removed campaignKey from DEFAULT_CHAR. Removed campaign key reading in confirmCreateCharacter(). Removed campaign badge from character selection screen cards. Removed campaign key from renderEditorForm() and readEditorInputs(). broadcastPresence() now reads campaignKey from config instead of character. loadConfigInputs() and saveConfig() now handle cfg-campaign-key. Fixed switchCharacter() race condition: Ably close is now deferred to the publish('leave') callback so the leave message is guaranteed to be delivered before the connection drops, removing the player from GM view immediately instead of after the 30s sweep timeout.
