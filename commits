fix: remove triplicated joincode-display spans from GM topbar after merge

Changes:

- views/aria-gm.html : merge conflict left three copies of id="joincode-display" and a leftover topbar-spacer in the topbar; removed the two spurious copies (lines 73 and 75) and the spacer, keeping only the correct one at the right side before the config button
