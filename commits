Pending commit

Changes:

- views/aria-player.html : Removed campaign key from character creation form and character editor (Personnage tab). Added campaign key input to the in-app config modal (⚙) so it is accessible without going into the character sheet.

- js/aria-player.js : campaignKey remains per-character (on the character object, not aria-config). loadConfigInputs() reads character.campaignKey into the config modal input. saveConfig() writes it back to character.campaignKey and calls saveCurrentCharacter(). Campaign badge restored on character selection cards. broadcastPresence() still uses character.campaignKey. Fixed switchCharacter() race condition: Ably close is deferred to the publish('leave') callback so the leave message is guaranteed delivered before the connection drops, removing the player from GM view immediately.
