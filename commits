feat: remove player count label, integrate vials into inventory list

Changes:

- views/aria-gm.html : Removed the "X/X joueur(s) en ligne" span from the
  topbar — the player cards themselves already show who is connected.

- js/aria-gm.js : Removed the two lines that updated #player-count text in
  renderPlayerCards().

- views/aria-player.html : Removed the separate #inv-vials-section div that
  sat above the inventory list.

- js/aria-player.js : Deleted renderVialsInInventory(). Integrated vials
  directly into renderInventoryEditor() — when playerTabs.alchemy is true, a
  "Fioles vides" row is appended to the inv-editor-list using the same inv-row
  grid as regular items, with inline −/count/+ buttons. renderInventorySidebar()
  now gates the fioles vides entry behind playerTabs.alchemy so the sidebar
  also updates correctly when the GM revokes the alchemy tab. changeVials() now
  calls renderInventoryEditor() + renderInventorySidebar() instead of the
  removed function. applyTabVisibility() calls renderInventoryEditor() so the
  vials row appears/disappears instantly when the GM toggles the alchemy tab.
